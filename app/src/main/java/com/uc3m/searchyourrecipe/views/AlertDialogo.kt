/*
package com.uc3m.searchyourrecipe.views

import android.R
import android.app.AlertDialog

import android.view.LayoutInflater




class AlertDialogo

fun createLoginDialogo(): AlertDialog? {
    val builder: AlertDialog.Builder = AlertDialog.Builder(this)
    val inflater: LayoutInflater = getActivity().getLayoutInflater()
    val v: View = inflater.inflate(R.layout.dialog_signin, null)
    builder.setView(v)
    val signup: Button = v.findViewById(R.id.crear_boton) as Button
    val signin: Button = v.findViewById(R.id.entrar_boton) as Button
    signup.setOnClickListener(
            object : OnClickListener() {
                fun onClick(v: View?) {
                    // Crear Cuenta...
                    dismiss()
                }
            }
    )
    signin.setOnClickListener(
            object : OnClickListener() {
                fun onClick(v: View?) {
                    // Loguear...
                    dismiss()
                }
            }
    )
    return builder.create()
}*/
